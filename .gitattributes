# Explicitly declare text files we want to always be normalized and
# converted to native line endings on checkout.
*.java       text
*.groovy     text
*.json       text
*.properties text
*.yml        text
*.xml        text
*.sql        text
README.md    text

# Make cvc-env.sh unix style EOL
*.sh eol=lf
# Make *.bat DOS style EOL
*.bat eol=crlf
*.csv eol=lf

# Denote all files that are truly binary and should not be modified.
*.zip binary
*.jar binary
*.war binary
